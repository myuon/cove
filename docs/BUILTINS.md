<!-- Generated by `cove reference`. Do not edit by hand.

     Every entry below is read out of `crates/cove-schema`, which is the one
     table the checker, both evaluators, and every diagnostic that lists what
     a type can do already read. Regenerate with `cove reference`; CI runs
     `cove reference --check` and fails if this file is stale. -->

# Builtins and the Host API

What the language answers to, and what a host answers to. Two tables, and the
line between them is the whole of what Cove can do without being given
anything: a **builtin** is the language's own, evaluated in the process with no
capability asked for; a **Host operation** reaches outside it and is refused
unless the run was granted the capability it names.

| | |
| --- | --- |
| builtin types | 18 |
| builtin methods | 97 |
| builtin associated functions | 13 |
| free builtins | 7 |
| host modules | 8 |
| host operations | 25 |

## Free builtins

Called on nothing, the way a declared function is. Every one of them binds its
own type parameters, because there is no receiver to read one off.

| name | kind | signature |
| --- | --- | --- |
| `Ok` | constructor | `Ok(value: T) -> Result<T, E>` |
| `Err` | constructor | `Err(error: E) -> Result<T, E>` |
| `Some` | constructor | `Some(value: T) -> Option<T>` |
| `Error` | constructor | `Error(message: String) -> Error` |
| `Shared` | constructor | `Shared(value: T) -> Shared<T>` |
| `assert` | assertion | `assert(condition: Bool) -> Result<Unit, Error>` |
| `assertEqual` | assertion | `assertEqual(actual: T, expected: T) -> Result<Unit, Error>` |

## Builtin types

### `Array<T>`

written as a namespace; binds `T`.

**Methods.**

| signature | `var self` | variadic | binds | implemented by |
| --- | --- | --- | --- | --- |
| `get(index: Int) -> Option<T>` |  |  |  | `machine` |
| `length() -> Int` |  |  |  | `machine` |
| `isEmpty() -> Bool` |  |  |  | `std.array.isEmpty` |
| `contains(element: T) -> Bool` |  |  |  | `machine` |
| `indexOf(element: T) -> Option<Int>` |  |  |  | `machine` |
| `slice(from: Int, to: Int) -> Array<T>` |  |  |  | `machine` |
| `map(transform: fn(T) -> R) -> Array<R>` |  |  | `R` | `machine` |
| `filter(keep: fn(T) -> Bool) -> Array<T>` |  |  |  | `machine` |
| `fold(initial: R, step: fn(R, T) -> R) -> R` |  |  | `R` | `machine` |
| `sorted(by: fn(T, T) -> Bool) -> Array<T>` |  |  |  | `machine` |
| `toVector() -> Vector<T>` |  |  |  | `machine` |
| `snapshot() -> Self` |  |  |  | `machine` |

### `Vector<T>`

written as a namespace; binds `T`.

**Associated functions.**

| signature | variadic | binds | implemented by |
| --- | --- | --- | --- |
| `of(items: T...) -> Vector<T>` | yes | `T` | `machine` |

**Methods.**

| signature | `var self` | variadic | binds | implemented by |
| --- | --- | --- | --- | --- |
| `get(index: Int) -> Option<T>` |  |  |  | `machine` |
| `length() -> Int` |  |  |  | `machine` |
| `isEmpty() -> Bool` |  |  |  | `std.vector.isEmpty` |
| `contains(element: T) -> Bool` |  |  |  | `machine` |
| `indexOf(element: T) -> Option<Int>` |  |  |  | `machine` |
| `slice(from: Int, to: Int) -> Array<T>` |  |  |  | `machine` |
| `map(transform: fn(T) -> R) -> Array<R>` |  |  | `R` | `machine` |
| `filter(keep: fn(T) -> Bool) -> Array<T>` |  |  |  | `machine` |
| `fold(initial: R, step: fn(R, T) -> R) -> R` |  |  | `R` | `machine` |
| `sorted(by: fn(T, T) -> Bool) -> Array<T>` |  |  |  | `machine` |
| `push(value: T) -> Unit` | yes |  |  | `machine` |
| `set(index: Int, value: T) -> Option<T>` | yes |  |  | `machine` |
| `pop() -> Option<T>` | yes |  |  | `machine` |
| `remove(index: Int) -> Option<T>` | yes |  |  | `machine` |
| `freeze() -> Array<T>` | yes |  |  | `machine` |
| `toArray() -> Array<T>` |  |  |  | `machine` |
| `snapshot() -> Self` |  |  |  | `machine` |

### `Map<K, V>`

written as a namespace; binds `K`, `V`.

**Associated functions.**

| signature | variadic | binds | implemented by |
| --- | --- | --- | --- |
| `of(entries: MapEntry<K, V>...) -> Map<K, V>` | yes | `K`, `V` | `machine` |

**Methods.**

| signature | `var self` | variadic | binds | implemented by |
| --- | --- | --- | --- | --- |
| `get(key: K) -> Option<V>` |  |  |  | `machine` |
| `length() -> Int` |  |  |  | `machine` |
| `isEmpty() -> Bool` |  |  |  | `std.map.isEmpty` |
| `contains(key: K) -> Bool` |  |  |  | `machine` |
| `keys() -> Array<K>` |  |  |  | `machine` |
| `values() -> Array<V>` |  |  |  | `machine` |
| `inserted(key: K, value: V) -> Map<K, V>` |  |  |  | `machine` |
| `removed(key: K) -> Map<K, V>` |  |  |  | `machine` |
| `snapshot() -> Self` |  |  |  | `machine` |

### `MapEntry<K, V>`

never written by name; binds `K`, `V`.

**Fields.** `key: K`, `value: V`

### `Set<T>`

written as a namespace; binds `T`.

**Associated functions.**

| signature | variadic | binds | implemented by |
| --- | --- | --- | --- |
| `of(items: T...) -> Set<T>` | yes | `T` | `machine` |

**Methods.**

| signature | `var self` | variadic | binds | implemented by |
| --- | --- | --- | --- | --- |
| `length() -> Int` |  |  |  | `machine` |
| `isEmpty() -> Bool` |  |  |  | `std.set.isEmpty` |
| `toArray() -> Array<T>` |  |  |  | `machine` |
| `contains(element: T) -> Bool` |  |  |  | `machine` |
| `inserted(element: T) -> Set<T>` |  |  |  | `machine` |
| `removed(element: T) -> Set<T>` |  |  |  | `machine` |
| `snapshot() -> Self` |  |  |  | `machine` |

### `String`

written as a namespace.

**Associated functions.**

| signature | variadic | binds | implemented by |
| --- | --- | --- | --- |
| `fromCodePoint(codePoint: Int) -> Result<String, Error>` |  |  | `machine` |

**Methods.**

| signature | `var self` | variadic | binds | implemented by |
| --- | --- | --- | --- | --- |
| `length() -> Int` |  |  |  | `machine` |
| `isEmpty() -> Bool` |  |  |  | `std.string.isEmpty` |
| `words() -> Array<String>` |  |  |  | `machine` |
| `chars() -> Array<String>` |  |  |  | `machine` |
| `split(separator: String) -> Array<String>` |  |  |  | `machine` |
| `join(parts: Array<String>) -> String` |  |  |  | `machine` |
| `slice(from: Int, to: Int) -> String` |  |  |  | `machine` |
| `trim() -> String` |  |  |  | `machine` |
| `contains(text: String) -> Bool` |  |  |  | `machine` |
| `startsWith(prefix: String) -> Bool` |  |  |  | `machine` |
| `endsWith(suffix: String) -> Bool` |  |  |  | `machine` |
| `indexOf(text: String) -> Option<Int>` |  |  |  | `machine` |
| `replace(old: String, new: String) -> String` |  |  |  | `machine` |
| `toUpper() -> String` |  |  |  | `machine` |
| `toLower() -> String` |  |  |  | `machine` |
| `snapshot() -> Self` |  |  |  | `machine` |

### `Range`

never written by name.

**Methods.**

| signature | `var self` | variadic | binds | implemented by |
| --- | --- | --- | --- | --- |
| `length() -> Int` |  |  |  | `machine` |
| `isEmpty() -> Bool` |  |  |  | `machine` |
| `contains(value: Int) -> Bool` |  |  |  | `machine` |
| `snapshot() -> Self` |  |  |  | `machine` |

### `Option<T>`

written as a namespace; binds `T`.

**Cases.** `Some(T)`, `None`

**Methods.**

| signature | `var self` | variadic | binds | implemented by |
| --- | --- | --- | --- | --- |
| `isSome() -> Bool` |  |  |  | `std.option.isSome` |
| `isNone() -> Bool` |  |  |  | `std.option.isNone` |
| `unwrapOr(fallback: T) -> T` |  |  |  | `std.option.unwrapOr` |

### `Result<T, E>`

written as a namespace; binds `T`, `E`.

**Cases.** `Ok(T)`, `Err(E)`

**Methods.**

| signature | `var self` | variadic | binds | implemented by |
| --- | --- | --- | --- | --- |
| `isOk() -> Bool` |  |  |  | `std.result.isOk` |
| `isError() -> Bool` |  |  |  | `std.result.isError` |
| `unwrapOr(fallback: T) -> T` |  |  |  | `std.result.unwrapOr` |
| `mapError(body: fn(E) -> F) -> Result<T, F>` |  |  | `F` | `std.result.mapError` |

### `Int`

written as a namespace.

**Associated functions.**

| signature | variadic | binds | implemented by |
| --- | --- | --- | --- |
| `parse(text: String) -> Result<Int, Error>` |  |  | `machine` |
| `parseRadix(text: String, radix: Int) -> Result<Int, Error>` |  |  | `machine` |

**Methods.**

| signature | `var self` | variadic | binds | implemented by |
| --- | --- | --- | --- | --- |
| `toFloat() -> Float` |  |  |  | `machine` |
| `abs() -> Int` |  |  |  | `machine` |
| `min(other: Int) -> Int` |  |  |  | `std.int.min` |
| `max(other: Int) -> Int` |  |  |  | `std.int.max` |
| `snapshot() -> Self` |  |  |  | `machine` |

### `Float`

written as a namespace.

**Associated functions.**

| signature | variadic | binds | implemented by |
| --- | --- | --- | --- |
| `parse(text: String) -> Result<Float, Error>` |  |  | `machine` |

**Methods.**

| signature | `var self` | variadic | binds | implemented by |
| --- | --- | --- | --- | --- |
| `toInt() -> Result<Int, Error>` |  |  |  | `machine` |
| `round() -> Float` |  |  |  | `machine` |
| `abs() -> Float` |  |  |  | `machine` |
| `min(other: Float) -> Float` |  |  |  | `machine` |
| `max(other: Float) -> Float` |  |  |  | `machine` |
| `format(digits: Int) -> String` |  |  |  | `machine` |
| `snapshot() -> Self` |  |  |  | `machine` |

### `Bool`

written as a namespace.

**Methods.**

| signature | `var self` | variadic | binds | implemented by |
| --- | --- | --- | --- | --- |
| `snapshot() -> Self` |  |  |  | `machine` |

### `Unit`

never written by name.

**Methods.**

| signature | `var self` | variadic | binds | implemented by |
| --- | --- | --- | --- | --- |
| `snapshot() -> Self` |  |  |  | `machine` |

### `Duration`

written as a namespace.

**Associated functions.**

| signature | variadic | binds | implemented by |
| --- | --- | --- | --- |
| `nanos(count: Int) -> Duration` |  |  | `machine` |
| `micros(count: Int) -> Duration` |  |  | `machine` |
| `millis(count: Int) -> Duration` |  |  | `machine` |
| `seconds(count: Int) -> Duration` |  |  | `machine` |
| `minutes(count: Int) -> Duration` |  |  | `machine` |
| `hours(count: Int) -> Duration` |  |  | `machine` |

**Methods.**

| signature | `var self` | variadic | binds | implemented by |
| --- | --- | --- | --- | --- |
| `nanos() -> Int` |  |  |  | `machine` |
| `micros() -> Int` |  |  |  | `machine` |
| `millis() -> Int` |  |  |  | `machine` |
| `seconds() -> Int` |  |  |  | `machine` |
| `minutes() -> Int` |  |  |  | `machine` |
| `hours() -> Int` |  |  |  | `machine` |
| `snapshot() -> Self` |  |  |  | `machine` |

### `Error`

written as a namespace.

**Fields.** `message: String`

### `Task<T>`

never written by name; binds `T`.

**Methods.**

| signature | `var self` | variadic | binds | implemented by |
| --- | --- | --- | --- | --- |
| `await() -> T` |  |  |  | `machine` |
| `cancel() -> Unit` |  |  |  | `machine` |

### `Shared<T>`

never written by name; binds `T`.

**Methods.**

| signature | `var self` | variadic | binds | implemented by |
| --- | --- | --- | --- | --- |
| `lock(body: fn(T) -> R) -> R` |  |  | `R` | `machine` |

### `Scope`

never written by name.

**Methods.**

| signature | `var self` | variadic | binds | implemented by |
| --- | --- | --- | --- | --- |
| `spawn(body: fn() -> T) -> Task<T>` |  |  | `T` | `machine` |

## Host API

Not builtins. Every operation below names a capability, is refused when the run
was not granted it, is charged against the run's Host-call budget, and appears
in a trace. A builtin does none of those things.

### `console`

Capability `console`.

| operation | capability | effect | cancellable |
| --- | --- | --- | --- |
| `println(String...) -> Result<Unit, Error>` | `console` | irreversible write |  |
| `print(String...) -> Result<Unit, Error>` | `console` | irreversible write |  |
| `eprintln(String...) -> Result<Unit, Error>` | `console` | irreversible write |  |
| `eprint(String...) -> Result<Unit, Error>` | `console` | irreversible write |  |

### `env`

Capability `env`.

| operation | capability | effect | cancellable |
| --- | --- | --- | --- |
| `get(String) -> Option<String>` | `env` | read |  |

### `documents`

Capability `documents`.

| operation | capability | effect | cancellable |
| --- | --- | --- | --- |
| `read(String) -> Result<String, Error>` | `documents` | read |  |

### `clock`

Capability `clock`.

| operation | capability | effect | cancellable |
| --- | --- | --- | --- |
| `now() -> Duration` | `clock` | read |  |
| `sleep(Duration) -> Result<Unit, Error>` | `clock` | read | yes |
| `timeout(Duration, Any) -> Result<Any, Error>` | `clock` | read | yes |
| `every(Duration, Any) -> Result<Unit, Error>` | `clock` | read | yes |

### `files`

Capability `files`.

| operation | capability | effect | cancellable |
| --- | --- | --- | --- |
| `read(String) -> Result<String, Error>` | `files` | read | yes |
| `write(String, String) -> Result<Unit, Error>` | `files` | irreversible write |  |
| `exists(String) -> Bool` | `files` | read | yes |
| `list(String) -> Result<Array<String>, Error>` | `files` | read | yes |
| `delete(String) -> Result<Unit, Error>` | `files` | irreversible write |  |
| `open(String) -> Result<files.Reader, Error>` | `files` | read | yes |
| `create(String) -> Result<files.Writer, Error>` | `files` | irreversible write |  |

- resource `files.Reader` — may not cross a task boundary
- resource `files.Writer` — may not cross a task boundary

### `process`

Capability `process`.

| operation | capability | effect | cancellable |
| --- | --- | --- | --- |
| `args() -> Array<String>` | `process` | read | yes |
| `exit(Int) -> Unit` | `process` | irreversible write |  |
| `run(String, Array<String>) -> Result<String, Error>` | `process` | irreversible write |  |

### `database`

Capability `database`.

| operation | capability | effect | cancellable |
| --- | --- | --- | --- |
| `query(String) -> Result<Array<String>, Error>` | `database` | read | yes |
| `connect(String) -> Result<database.Connection, Error>` | `database` | reversible write |  |

- resource `database.Connection` — may cross a task boundary

### `http`

Capability `http`.

| operation | capability | effect | cancellable |
| --- | --- | --- | --- |
| `fetch(String) -> Result<http.Response, Error>` | `http` | read | yes |
| `json(Int, Any) -> http.Response` | `http` | read |  |
| `listen(Int) -> Result<http.Server, Error>` | `http` | reversible write |  |

- type `http.Method` — `Method()`
- type `http.Request` — `Request(method: http.Method, path: String, body: String)`
- type `http.Response` — `Response(status: Int, body: String)`
- type `http.Route` — `Route(method: http.Method, path: String, handler: Any)`
- resource `http.Server` — may cross a task boundary

