expression negative zero positive
bound 1
statement one
statement with else other
an `if` with no `else` and a false condition is ()
its branch ran: true
an `if` with no `else` whose branch ran is ()
