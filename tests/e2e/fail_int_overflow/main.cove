use console.println

/// Adds one past the largest `Int`.
export fn main() -> Result<Unit, Error> {
  let largest = 9223372036854775807
  println("largest {largest}")?
  println("never printed {largest + 1}")?
  Ok(())
}
