use console.println
use files

/// Reads a file in a run that was granted `console` but not `files`.
export fn main() -> Result<Unit, Error> {
  println("console was granted")?
  println("the notes are {files.read("notes.txt")}")?
  Ok(())
}
