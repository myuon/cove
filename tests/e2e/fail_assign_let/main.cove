use console.println

/// Assigns to a `let` place, which is read-only.
export fn main() -> Result<Unit, Error> {
  let limit = 10
  println("limit {limit}")?
  limit = 20
  println("never printed {limit}")?
  Ok(())
}
