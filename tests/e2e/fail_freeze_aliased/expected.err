error[cove::unique::not_unique]: `freeze()` cannot prove that `building` holds the only handle to its storage
 --> <e2e>/fail_freeze_aliased/main/main.cove:9:16
  |
9 |   let frozen = building.freeze()
  |                ^^^^^^^^^^^^^^^^^
 --> <e2e>/fail_freeze_aliased/main/main.cove:6:15
  |
6 |   var alias = building
  |               ^^^^^^^^ `building` is copied into another binding here
  rule: `freeze()` consumes a vector whose storage the compiler can prove is uniquely owned here, and returns an immutable array in O(1).
  help: call `toArray()` on the vector instead, which copies the elements in O(n) and asks nothing about who else is holding it
1 error(s)
