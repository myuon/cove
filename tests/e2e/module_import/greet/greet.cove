use console.println

/// Returns a greeting for `name`.
export fn greeting(name: String) -> String {
  "Hello, {name}!"
}

/// Prints a greeting for `name`.
export fn say(name: String) -> Result<Unit, Error> {
  console.println(greeting(name))
}
